# CLAUDE.md

台美股報價與持股市值專案。目前的主線是**把既有本機系統部署到雲端**（Cloudflare Workers 前端 ＋ Cloud Run Python API ＋ Supabase PostgreSQL）。

## 先讀進度，再動手

**`docs/cloud-phase-1-plan.md` 的抬頭是唯一的進度來源。** 它是一張現況表，標明下一步、每個步驟的狀態、以及接手前必讀的硬性限制，並連到各步驟的證據文件。抬頭與文件下方的舊段落衝突時以抬頭為準。

編號慣例：決策 `D1`–`D6`、執行步驟 `C1`–`C7`、步驟內項目如 `C4-1`。證據檔為 `docs/cloud-CN-evidence.md`，POC 時期為 `docs/step-N-evidence.md`。

## 這個專案對「驗證過」的定義很嚴格

這是本專案最重要的一條規則，違反它比寫錯程式更糟：

- **未實測的事不得在任何文件中宣稱已驗證。** 勾選代表交付完成且**具驗證證據**。
- **推論不算實測。** 若只做了設定查核或正向測試，要寫明「未做反面測試」「未實測」。
- **量測要量真實路徑。** 例：`C7-6` 明文規定「公司網路測試通過不能證明 GCP 出口可用」。
- **不得寫入推測值。** `D3` 禁止在量測完成前把猜測的數字寫進程式或部署設定。缺數值時保持未定，並寫明何時、由哪一項補上。
- **缺陷、失敗與偶發都要記錄，不要只記成功。** 排除某個異常時要寫出排除的依據。

寫文件時偏好「就地補記」而非改寫，保留當初的決策依據；改寫會讓後人看不出結論變過。

## 測試

本機 `pytest` 只涵蓋離線案例，資料庫整合案例會 skip——**本機通過不等於測試通過**。

要宣稱完整套件通過，一律照 `docs/step-3-evidence.md` 的隔離容器程序跑（拋棄式 PostgreSQL ＋ runner container，含該文件記錄的兩個已知坑）。該程序下應為 **0 skipped**。

新增測試時：讓測試**在修正前會失敗**，且失敗原因要是行為斷言而非 API 不存在。若上限、預算之類的約束在測試情境下根本碰不到，那個測試什麼都沒守住。

## 提交

- subject 用 `[type] 中文描述`，type 為 `feat`／`fix`／`docs`／`test`／`ci`。
- body 寫**為什麼**與**怎麼發現的**，不只寫改了什麼；缺陷要寫出成因、影響範圍、以及為何先前沒被發現。
- 推送到 `main` 會觸發建置並推映像（`.github/workflows/build-image.yml`），純 `docs/` 變更不會。

## 不要做的事

- **不要對 `finpo` Cloudflare Worker 部署任何東西**，直到 `C7-1` 執行。它承載 `C1-6` 的 canary，是目前唯一能驗證 Access 生效的東西。
- **不要在 `deploy/cloud.toml` 調高 `refresh_deadline_seconds`**，除非重新量過 Cloudflare 的邊緣上限。該檔內有完整算式。
- **不要把常駐 monitor 的概念帶回雲端設計。** 第一階段明文不納入常駐 monitor、七天觀測與固定排程抓價；抓價改為在使用者請求內完成（`D3`）。

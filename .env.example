# Template only. Do not put real credentials in this tracked file.
# These values will be injected by the execution environment at integration time.
# The CLI scaffold does not load .env automatically.
DB_PASSWORD=
FINNHUB_API_KEY=
